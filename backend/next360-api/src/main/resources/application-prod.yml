# ============================================================
# Next360 — Production Profile
# ============================================================

spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    com.next360: INFO
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
