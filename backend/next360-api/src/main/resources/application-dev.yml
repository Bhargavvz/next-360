# ============================================================
# Next360 — Development Profile
# ============================================================

spring:
  jpa:
    show-sql: true

logging:
  level:
    com.next360: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
